describe('HighlightDirective', () => {
  it('should add class up to element if value higher than prev', () => {
    // TODO:
  });

  it('should add class up to element if value higher than prev', () => {
    // TODO:
  });
});
